enum BlockType {
  keyword,
  type,
  name,
  value,
  operator,
}

enum BlockColor {
  orange,
  cyan,
  yellow,
  purple,
  green,
}

enum InventoryFilter {
  all,
  keyword,
  type,
  name,
  value,
  operator,
}
